package com.example.koadex.ui.test
/*
import com.example.koadex.ViewModels.FormularioEspeciesViewModel
import junit.framework.TestCase.assertEquals
import org.junit.Test


class NumberViewModelTest {
    private val viewModel = FormularioEspeciesViewModel()

    @Test
    fun inputNumberViewModel() {
        assertEquals(1, viewModel.checkEnteroInput(0))
        assertEquals(2, viewModel.checkEnteroInput(-2))
        assertEquals(3, viewModel.checkEnteroInput(3))
        assertEquals(1, viewModel.checkEnteroInput(0))
    }

    @Test
    fun plusNumberViewModel() {
        viewModel.updateEnteroInput(5)
        viewModel.enteroMasUno()
        assertEquals(6, viewModel.entero)
    }

    @Test
    fun minusNumberViewModel() {
        viewModel.updateEnteroInput(5)
        viewModel.enteroMenosUno()
        assertEquals(4, viewModel.entero)
        viewModel.updateEnteroInput(1)
        viewModel.enteroMenosUno()
        assertEquals(1, viewModel.entero)
    }
}

 */